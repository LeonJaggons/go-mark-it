import { Heading } from "@chakra-ui/react";
import React from "react";

const Saved = () => {
    return (
        <div>
            <Heading size={"lg"}>Saved</Heading>
        </div>
    );
};

export default Saved;
